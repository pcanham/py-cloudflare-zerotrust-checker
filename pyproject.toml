[project]
name = "cloudflare-zerotrust-checker"
version = "0.1.0"
description = ""
authors = [
    {name = "Paul Canham", email = "paul@canham.io"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "celery[redis]>=5.6.3",
    "requests>=2.33.1",
    "python-dotenv>=1.2.2",
    "flask-bootstrap>=3.3.7.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
